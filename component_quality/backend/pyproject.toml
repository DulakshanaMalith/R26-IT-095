[project]
name = "exposia"
version = "0.1.0"
description = "Robust Data-Processing Pipeline for the Exposia 1.0.0 Dataset"
dependencies = [
    "pandas",
    "pyarrow",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
